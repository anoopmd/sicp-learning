## SICP
Notes & some programs from the SICP series

### Commands
```bash
# Install on Ubuntu
sudo apt-get install -y mit-scheme

# Enter repl mode
scheme

# Exit from repl
ctrl+z

# Run file
scheme < filename.scm
```